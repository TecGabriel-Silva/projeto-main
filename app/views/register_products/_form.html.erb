<%= form_with(model: register_product) do |form| %>
  <% if register_product.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(register_product.errors.count, "error") %> prohibited this register_product from being saved:</h2>

      <ul>
        <% register_product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :Nome, style: "display: block" %>
    <%= form.text_field :titulo %>
  </div>

  <div>
    <%= form.label :Descriçao, style: "display: block" %>
    <%= form.text_area :descricao %>
  </div>

  <div>
    <%= form.label :Valor, style: "display: block" %>
    <%= form.text_field :valor %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
